Runtime: 20 ms, faster than 98.78% of C++ online submissions for Transpose Matrix.
Memory Usage: 11.6 MB, less than 61.54% of C++ online submissions for Transpose Matrix.
```
class Solution {
public:
    vector<vector<int>> transpose(vector<vector<int>>& A) {
        int m=A.size();
        int n=A[0].size();
        vector<vector<int>>res(n,vector<int>(m));
        for(int i=0 ;i<m; ++i){
            for(int j=0; j<n ;++j){
                res[j][i]=A[i][j];
            }
        }
        return res;
    }
};
```
